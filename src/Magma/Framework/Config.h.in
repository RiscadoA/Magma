#if ${MAGMA_FRAMEWORK_USE_DIRECTX} == 1
#define MAGMA_FRAMEWORK_USE_DIRECTX
#endif

#if ${MAGMA_FRAMEWORK_USE_OPENGL} == 1
#define MAGMA_FRAMEWORK_USE_OPENGL
#endif

#if ${MAGMA_FRAMEWORK_USE_OPENAL} == 1
#define MAGMA_FRAMEWORK_USE_OPENAL
#endif

#if ${MAGMA_FRAMEWORK_DEBUG} == 1
#define MAGMA_FRAMEWORK_DEBUG
#endif

#if ${MAGMA_FRAMEWORK_USE_STD_FILESYSTEM} == 1
#define MAGMA_FRAMEWORK_USE_STD_FILESYSTEM
#endif