#if ${MAGMA_FRAMEWORK_USE_DIRECTX} == 1
#define MAGMA_FRAMEWORK_USE_DIRECTX
#endif

#if ${MAGMA_FRAMEWORK_USE_OPENGL} == 1
#define MAGMA_FRAMEWORK_USE_OPENGL
#endif

#if ${MAGMA_FRAMEWORK_USE_OPENAL} == 1
#define MAGMA_FRAMEWORK_USE_OPENAL
#endif

#if ${MAGMA_FRAMEWORK_DEBUG} == 1
#define MAGMA_FRAMEWORK_DEBUG
#endif

#if ${MAGMA_FRAMEWORK_USE_WINDOWS_FILESYSTEM} == 1
#define MAGMA_FRAMEWORK_USE_WINDOWS_FILESYSTEM
#endif

#if ${MAGMA_FRAMEWORK_USE_WINDOWS_THREADS} == 1
#define MAGMA_FRAMEWORK_USE_WINDOWS_THREADS
#endif

#define MAGMA_ROOT_DIRECTORY u8"${MAGMA_ROOT_DIRECTORY}"
